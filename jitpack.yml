# JitPack configuration for CASL Android
# https://jitpack.io/docs/BUILDING/

jdk:
  - openjdk17

before_install:
  - sdk install java 17.0.1-open || true
  - sdk use java 17.0.1-open

install:
  - ./gradlew clean build publishToMavenLocal -x test
